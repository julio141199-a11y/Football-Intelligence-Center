# Chat/Codex Vacancy Inbox

Place one JSON object or an array of vacancy objects here, then run:

`python scripts/import_vacancies.py data/inbox`

Only Head Coach and Assistant Coach records with a real public source URL are accepted. Placeholder URLs are rejected.
